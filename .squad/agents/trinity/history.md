# Trinity — History

## Core Context

- **Project:** A C# WinForms (.NET 10) desktop application that enhances GitHub Copilot workflow with session management, IDE integration, and GitHub tracking.
- **Role:** Services Dev
- **Joined:** 2026-03-15T15:50:59.673Z

## Learnings

<!-- Append learnings below -->
- **2026-05-03 — All-green integration directive:** All integration tests must be GREEN at all times. No tolerance for environmental baseline failures. Tests that fail due to missing setup (e.g., Playwright not installed locally) are TEST BUGS requiring self-bootstrapping (fixture installs) or explicit skip traits. This supersedes any prior decision to "document pain" or "tolerate named-diff baseline".
- **2026-03-16 — Phase 3 trigger wiring:**Wired all 5 triggers for Copilot Host resolution. T1: `CopilotLogWatcherService.ExternalSessionDiscovered` now passes `(sessionId, copilotPid)` signature (was `sessionId` only). T2: Added `PidRegistryService.CopilotPidRegisteredStatic` static event with dedup logic via `s_lastCopilotPidByLauncherPidStatic` dictionary to avoid firing on duplicate updates. Static path needed because `Program.cs` calls `UpdatePidSessionId` statically. T4: `ActiveStatusTracker.FullRefresh` now iterates `SessionService.GetActiveSessions()` after cleanup, re-resolving hosts for sessions with `CopilotPid > 0` where host is missing or dead. T5: `WindowEventHookService.WindowDestroyed` already existed; wired to `ActiveStatusTracker.HandleWindowDestroyed` which evicts `_copilotHosts` entries by HWND match. Focus migration: `TryFocusCopilotCli` and `FocusActiveProcess` now check `_copilotHosts` first (Priority 1), then legacy title-scan `_activeTrackedWindows` (Priority 2), then PID-based fallback (Priority 3). `FocusActiveProcess` skips duplicate HWND insertion when host already added. All 647 unit tests pass, 0 warnings/errors across all 3 projects, `dotnet format` clean.
- **2026-03-15 — Issue #12 service design:** Designed async layer for git worktree operations. Key decisions: `CancellationToken`-only (no `timeoutMs` param — caller owns policy via `CancellationTokenSource(TimeSpan)`), `IProgress<string>` for stderr streaming, `GitResult` record struct over raw tuples, `RunGitAsync` promoted to `internal static` for testability. Sync methods kept unchanged — async overloads are additive. `GitService` being all-static with local-only state is safe for async — no thread-safety concerns. `WorkspaceCreationService` async methods keep sync helpers for fast metadata reads, only the worktree/fetch calls go async.
- **Process model:** `RunGit` (line 502) creates a new `Process` per call — fully isolated. `WaitForExitAsync` available since .NET 5+. Cancellation callback registers `Kill(entireProcessTree: true)` wrapped in try/catch for race conditions.
- **UI integration note:** `WorkspaceCreatorVisuals.cs` currently wraps sync calls in `Task.Run()` (line 1129). Phase 2 will replace this with direct `await` on async overloads — Morpheus owns that work.
- **Callers with custom timeouts:** `ValidatePrRef` (30s, line 434) and `FetchPrRef` (60s, line 450) already override the default. The three worktree callers (lines 110, 124, 134) never did — that's the root cause.
- **2026-03-15 — Phase 1+2 implementation:** Implemented `RunGitAsync` (internal static, tuple return, no hard timeout, concurrent stream reads, cancellation callback kills process tree before awaiting). Added 4 async wrappers: `CreateWorktreeAsync`, `CheckoutExistingBranchWorktreeAsync`, `CheckoutLocalBranchWorktreeAsync`, `FetchPrRefAsync`. Added 3 async overloads in `WorkspaceCreationService`: `CreateWorkspaceAsync`, `CreateWorkspaceFromExistingBranchAsync`, `CreateWorkspaceFromPrAsync`. All sync methods untouched. Fixed pre-existing tests that assumed `GitResult` record — corrected to tuple member names and `ThrowsAnyAsync` for `TaskCanceledException` (subclass of `OperationCanceledException` thrown by `ReadToEndAsync`).
- **Key implementation detail:** `ReadToEndAsync(CancellationToken)` in .NET 10 throws `TaskCanceledException` (not `OperationCanceledException` directly). Tests must use `ThrowsAnyAsync<OperationCanceledException>` for correct assertion.
- **2026-03-16 — 0.21.0 Phase 1 foundations:** Implemented Phase 1 API surface for Copilot Host discovery and Booster-Resolved Names. Created five new components: `HostKindClassifier` (process name → friendly label with case-insensitive matching for 11 host kinds), `BoosterResolvedNameFormatter` (32-char truncation with ellipsis, whitespace collapse, code fence stripping, uses `GeneratedRegex` for perf), `FirstUserMessageExtractor` (forward line-by-line reader from events.jsonl, stops at first user.message, tolerates malformed JSON), `SessionNameOverrideService` (JSON sidecar service mirroring SessionAliasService pattern with `SessionNameOverride` record), and `CopilotHostInfo` record (5-property value type: HostHwnd, HostPid, CopilotPid, HostProcessName, HostKindLabel). Added `Program.SessionNameOverrideFile` constant. All services follow the trinity charter: static classes, `internal` visibility, member ordering conventions enforced by dotnet format.
- **2026-03-15 — Phase 2 host discovery primitives:** Implemented `IProcessTreeProvider` abstraction for testability with `Win32ProcessTreeProvider` using Toolhelp32 snapshots (`CreateToolhelp32Snapshot`, `Process32First/Next`, PROCESSENTRY32 struct). `GetParentPid` uses `th32ParentProcessID` field (more reliable than WMI). `GetProcessName` wraps `Process.GetProcessById(pid).ProcessName` in try/catch. `GetTopLevelWindow` delegates to `WindowFocusService.FindWindowHandleByPid` to reuse existing EnumWindows logic. `CopilotHostResolver` walks parent tree with cycle detection (max 32 ancestors), skips `ownPid` parameter, returns first ancestor with a visible top-level window. `HostKindClassifier` maps process names to human-readable labels (e.g., "WindowsTerminal" → "Windows Terminal", falls back to "Unknown"). Extended `WindowHandleCacheService.HandleEntry` with optional `HostPid` and `CopilotPid` fields (default null for backward compatibility). Added "copilot-host" entry type that repurposes `FolderPath` field to store `HostKindLabel` (documented in code comment). New `Save` overload accepts `Dictionary<string, CopilotHostInfo>` for copilot-host entries. `Load` returns 5-tuple with `CopilotHosts` dictionary, validates host entries via `GetWindowProcessId(hwnd) == HostPid` check. Updated `ActiveStatusTracker` Load call site to destructure 5-tuple (added `cachedHosts` variable, currently unused — Phase 3 will wire it).
- **2026-03-16 — Phase 1 Booster-Resolved Name resolution chain:** Implemented unified display name resolution chain in `SessionService.LoadNamedSessions` and `ParseWorkspace`. Extended `LoadNamedSessions` signature with optional `aliasFile` and `overrideFile` parameters (default null). Load both dictionaries once at top of method (similar to `deletedIds` pattern) to avoid per-session I/O. Resolution priority (highest first): Alias (from `SessionAliasService`) → workspace.yaml summary → SessionNameOverride (from sidecar) → cwd folder (displaySummary = "") → literal "(no summary)". **Alias call site discovery:** Previously, `SessionRefreshCoordinator.LoadSessions` called `LoadNamedSessions` then overlaid alias onto `.Alias` field in a separate loop. Now alias is resolved directly into `.Summary` field within `LoadNamedSessions` (priority 1 in chain). Removed the post-load alias overlay loop from `SessionRefreshCoordinator`. Updated `ParseWorkspace` to read from `Program.SessionNameOverrideFile` and apply same chain (except alias, which is not available in that hot path). Folder fallback behavior preserved: when folder exists and summary is empty, sets displaySummary = "" so UI can show folder badge separately. All 636 unit tests pass.
- **2026-03-16 — Phase 3 scaffolding:** Added `_copilotHosts` dictionary and `_hostResolver` field to `ActiveStatusTracker`. Implemented three accessor methods: `GetCopilotHost`, `SetCopilotHost`, `RemoveCopilotHost`. Wired `WindowHandleCacheService.Load` to populate `_copilotHosts` and `Save` to persist it (5th parameter). Host entries project into `_activeTrackedWindows` as `("Copilot CLI", "", hwnd)` tuples via `ProjectCopilotHostToActiveWindows` with HWND-based deduplication. `UnprojectCopilotHostFromActiveWindows` removes entries by HWND match. Added `CopilotHostResolved` and `CopilotHostRemoved` events for downstream tasks. Projection target dict: `_activeTrackedWindows` (stores `List<(string Label, string Title, IntPtr Hwnd)>` per session). Dedup rule: scan existing entries, skip insert if HWND already present, preserving title-scan entries.
- **2026-03-16 — Phase 4 UIA gateway implementation:** Implemented `WindowsTerminalPaneGateway` using `System.Windows.Automation` to enumerate Windows Terminal tabs via UIAutomation. Enabled `<UseWPF>true</UseWPF>` in `CopilotBooster.csproj` — direct `<Reference Include="UIAutomationClient" />` approach failed in .NET 10 with assembly resolution errors. UseWPF transitively provides UIAutomationClient and UIAutomationTypes assemblies. Binary size impact not measured (Phase 3 code incomplete, preventing full build). Implementation: `AutomationElement.FromHandle(wtHwnd)` → `FindAll(TreeScope.Descendants, ControlType.TabItem)` → 250ms time-box with `Stopwatch`. Each Select action tries `SelectionItemPattern.Select()` first, falls back to `InvokePattern.Invoke()`, swallows `ElementNotAvailableException` and `InvalidOperationException`. Top-level try/catch logs warning and returns empty list on any UIA failure.
- **Playwright self-bootstrap pattern (0.21.0 Round 4):** Local IT runs auto-install chromium via xUnit collection fixture. Probe `Playwright.CreateAsync().Chromium.LaunchAsync()` once per test process; on missing browser call `Microsoft.Playwright.Program.Main(["install", "chromium"])`. First run 36s (with install), cached 29s, vs. 39s pre-bootstrap with 13 reds. Fixture is transparent — CI's existing `playwright install chromium` release step makes the fixture a fast probe.
- **2026-03-16 — Phase 3 summary write cleanup and deferred name resolution:** Modified `CopilotLogWatcherService.CreateWorkspaceYamlFromPid` (line ~401) to comply with ADR-0001: removed GUID fallback (`?? sessionId`) from summary write. Now passes `GetWindowTitleByPid(pid) ?? string.Empty` to the shared `CreateWorkspaceYaml` helper (line ~369), which checks `!string.IsNullOrWhiteSpace(sessionName)` before writing `summary:` and `name:` fields. External sessions discovered via log watcher never write a GUID to `workspace.yaml.summary`; the T1 trigger (sister task) sets the Booster-Resolved Name placeholder in the sidecar instead. Added deferred name resolution to `EventsJournalService.OnFileChanged`: new `TryResolveBoosterName` method (line ~440) checks if the current override has `ResolvedFromUserMessage == false`, extracts first user.message via `FirstUserMessageExtractor.Extract`, formats via `BoosterResolvedNameFormatter.Format`, and updates sidecar with `resolvedFromUserMessage: true`. New event `BoosterResolvedNameUpdated` (Action<string>) fires on successful resolution for MainForm to trigger UI refresh. Resolution is short-circuited once `ResolvedFromUserMessage == true` (no redundant extraction). Updated `ExternalSessionDiscovered` event signature from `Action<string>` to `Action<string, int>` (added copilotPid parameter) for consistency with ActiveStatusTracker T1 trigger expectations.
