# Oracle — History

## Core Context

- **Project:** A C# WinForms (.NET 10) desktop application that enhances GitHub Copilot workflow with session management, IDE integration, and GitHub tracking.
- **Role:** Quality Architect
- **Joined:** 2026-03-15T15:50:59.677Z

## Learnings

<!-- Append learnings below -->

- **2026-05-10 — Team update: WarpPaneFocuser landed and title-probe-tab-focus skill documented:** Trinity shipped deterministic R2 probe-and-match Warp terminal pane focus (7 new service files with seam architecture, 10 constructor overloads for backward compat). Tank shipped 12 unit + 3 LocalOnly live integration tests, all green against Roger's live Warp Hi 1 / Hi 2. Strategy: Ctrl+Tab cycling with title match detection (30 iteration cap, 150ms settle). Session display name sourced from SessionInfo.Summary. Fallback: focus warp.exe window on mismatch (safety net). Skill extracted to `.squad/skills/title-probe-tab-focus/SKILL.md` for future terminal work. Decisions merged into decisions.md (squad-warp-r2-pivot, trinity-warp-pane-focuser, tank-warp-r2-tests). Build clean (0 warn, 0 err), 851 unit tests (+28 from baseline), 141 integration tests.
- **2026-05-03 — All-green integration test directive (quality architecture):** User grilling on IT regression risk exposed process smell: team was accepting baseline failures as "normal" and building ceremony (baseline-comparison scripts) around test-output noise. User directive: binary green only. All 104 IT must pass at all times. Tests failing due to environmental gaps (missing Playwright) are TEST BUGS, not "known baselines". Fix via fixture auto-install or explicit skip-traits. Supersedes prior tolerance decisions.
- **2026-05-14 — CWD reactive update plan architect review:** Decomposed locked plan into 7 work items (3 Trinity, 1 Morpheus, 3 Tank). Key architectural findings: (1) `TryParseLogContent` is a pure static parser — PEB probe must NOT be inserted inside it; apply at caller `TryProcessLogFile` instead. (2) `ValidateCwdOrPrompt` (MainForm.cs:1983) is NOT editor-driven — its `UpdateSessionCwd` call stays. (3) Win32ProcessCwd must use `SafeProcessHandle` for guaranteed cleanup. (4) 64-bit PEB only is fine since Copilot CLI is always 64-bit Node.js. (5) Existing test `TryParseLogContent_UsesUserProfileFallback_WhenNoCwdAnywhere` will break and needs update. (6) Alias field editability is ambiguous in the plan — flagged for Neo. Files: `CopilotLogWatcherService.cs` (lines 181, 386), `MainForm.ContextMenu.cs` (lines 41-65), `SessionEditorVisuals.cs`, `SessionService.cs:504`. Analysis written to `.squad/decisions/inbox/oracle-cwd-plan-architect-review.md`.
- **2026-05-14 — TDD gate review (WI-2/WI-3):** Reviewed Tank's RED test reports via code reasoning (no dotnet run). **WI-2:** Approved 11 failing tests — all fail for correct reasons (UserProfile fallback in pure parser, missing `ResolveCwdAfterParse` seam). Recommended seam adjustments: normalize whitespace, guard relative paths, handle PEB whitespace. No blocking false-positive risks. **WI-3:** Ruled CLOSED-AS-ABSORBED — the 4 source-contract tests are valid regression guards but impl was absorbed into WI-4 before TDD directive. Accepted deviation since tests serve intended purpose and forcing revert-redo wastes time. Verdict: Trinity proceeds with WI-2; WI-3 closed.
- **2026-05-14 — WI-2 implementation review (GREEN):** Approved Trinity's WI-2 implementation. All spec requirements verified: (1) UserProfile fallback removed from `TryParseLogContent`, now returns `""`. (2) `ResolveCwdAfterParse` seam added with exact signature and all four Oracle normalizations (parsedCwd UserProfile check, PEB whitespace guard, DefaultWorkDir relative path guard via `Path.IsPathRooted`, defensive try/catch). (3) Seam correctly wired before `CreateWorkspaceYamlFromPid`. **Deviation accepted:** `s_gitRepoCache` changed from `Dictionary` to `ConcurrentDictionary` to fix parallel test race. This is the correct fix for shared static state under xUnit parallel execution. Extra file changes verified as cosmetic (dotnet format BOM fixes, unused using removals). Suite: 939 unit, 155 integration, all green. WI-2 closed.

- **Team update 2026-05-14 — CWD feature complete:** Feature closed with 7 WI across 4 agents. Tank's 15 tests (11+4) all green. Trinity's implementation approved, all normalizations applied. WI-3 ruled closed-as-absorbed. Full suite green (939 unit, 155 integration, 0 failed). Build clean. Decisions merged, inbox cleared. Feature ready for release.

- **2026-05-16 — TDD gate review (Live CWD extraction from events.jsonl):** Reviewed Tank's 7 RED tests for WI-LiveCwd-1. All tests fail for correct reasons: 6 fail at `Assert.NotNull(method)` because static `ExtractLatestCwd(TextReader)` doesn't exist; 1 fails at `Assert.NotNull(ctor)` because `EventsJournalService(string sessionsDir)` doesn't exist. Test fixtures correctly use `session.start.data.context.cwd` and `hook.start.data.input.cwd` JSON paths. Truncated-final-line scenario is realistic (watcher fires mid-write). Approved all 3 seams: (1) pure parser `ExtractLatestCwd`, (2) event `LatestCwdChanged(sessionId, cwd)`, (3) testable ctor with explicit requirement to preserve parameterless overload for backward compat with `ActiveStatusTracker.cs:46`. Read-only directive verified: tests write to `AppContext.BaseDirectory` temp dir, not `~/.copilot/session-state/`. **Verdict: APPROVED.** Trinity proceeds.

- **2026-05-16 — WI-LiveCwd-1 implementation review (GREEN):** Approved Trinity's implementation. All spec requirements verified: (1) `ExtractLatestCwd(TextReader)` pure parser (line 271), (2) `LatestCwdChanged` event (line 52), (3) both constructors (lines 70–78) with backward compat preserved. Behavior verified: scans lines sequentially, prefers `hook.start.data.input.cwd` over `session.start.data.context.cwd`, skips malformed JSON via `catch (JsonException)`, uses `FileShare.ReadWrite` for concurrent reads. ConcurrentDictionary uses `OrdinalIgnoreCase` for Windows path semantics. Suppress flow honored (`raiseCwdChanged: !this.SuppressEvents`). MainForm wiring correct: `BeginInvoke` marshals to UI thread, `IsHandleCreated` guard matches existing patterns. **Deviation accepted:** Test reflection helper uses `GetAddMethod(nonPublic: true)` because `EventInfo.AddEventHandler` requires public add accessor, but event is `internal`. This is the standard .NET reflection pattern for subscribing to internal events. Deviation lives in test code only. Suite: 946 unit, 155 integration, all green. **Verdict: APPROVED. WI-LiveCwd-1 closed.**

- **Team update 2026-05-16 — Live CWD from events.jsonl:** Completed WI-LiveCwd-1. Tank's 7 RED tests now GREEN. Implemented `ExtractLatestCwd(TextReader)` parser with JSONL line scan, per-session latest-cwd cache, and `LatestCwdChanged` event. Wired to MainForm: `OnLatestCwdChanged` marshals with `BeginInvoke`, updates `NamedSession.Cwd` and `Folder`, calls `RequestRefresh`. Backward compat preserved: parameterless ctor chains to new `string sessionsDir` overload. Read-only enforced: production reads only (FileShare.ReadWrite), tests use temp dirs. Reflection deviation in test helper (GetAddMethod nonPublic:true) accepted as standard .NET pattern. Feature closed (946 unit, 155 integration).

- **2026-05-16 — TDD gate review (Live CWD survives reload):** Reviewed Tank's RED test for production reload path. **Key learning: prior test round missed the pipeline reload.** WI-LiveCwd-1 tests covered the parser seam and event seam independently, but not the production `OnDebouncedRefreshAsync` path that calls `LoadNamedSessions` then overlays live CWD from cache. Tank's new test `LatestCwdChanged_ThenFreshLoadCanBeOverlayedWithCachedLiveCwd` closes that gap. Verified: (1) same `LoadNamedSessions` overload as production, (2) JSON fixtures accurate (session.start.data.context.cwd, hook.start.data.input.cwd, proper escaping), (3) fails for correct reason (LoadNamedSessions returns stale YAML cwd, overlay step required), (4) both Cwd and Folder asserted, (5) IDisposable cleanup robust, (6) LocalOnly trait appropriate for watcher timing, (7) read-only compliant (temp paths only). No new seam needed: production already applies overlay at MainForm.cs:1752-1760. **Verdict: APPROVED.** Trinity may proceed (no code changes required; production implementation already correct).

- **2026-05-17 — TDD gate review (Cache-free CWD RED suite):** Reviewed Tank's 31-test cache-free suite (19 honest RED, 12 passing). **Verdict: APPROVE.** All directive bullets encoded with tests (NO derivative caches, fresh per-refresh reads, mtime-based resolution, tail-read budget, watcher triggers, quote parsing, overlay removal, stale test cleanup). Verification: build clean, all tests correctly fail for missing production behavior (not test bugs). Source-contract guards adequate (field name + implementation shape + API surface). Performance budget (500KB tail-read on 8MB synthetic) is discriminating (16× gap vs full-file load). **Implementer assignment: Switch.** Per ULTRA 2026-05-15T18-50-00Z (binary verdicts), Dozer's gap-finding round forced lockout of original author; Switch hired as relief. Must deliver all 31 tests GREEN.

- **2026-05-17 — GREEN gate review (Live CWD overlay production seam): APPROVED.** Trinity's implementation matches the binding contract exactly: signature, owner, static, parameters, all defensive guards. Verified independently: builds clean, 7/7 targeted tests pass, 956/956 full suite pass (2 skipped). MainForm callsite ordering correct (LoadSessions → ApplyLiveCwdOverlay → cache → ApplySessionStates). Zero allocations on no-op path. Minor test-only scope expansion (reflection removal in CwdTests) accepted as beneficial quality improvement. **Principle: A GREEN that matches the RED contract character for character and adds zero no-op allocations is the gold standard for overlay seams.**

- **2026-05-17 — Switch GREEN gate review (Live CWD overlay, gap closure): APPROVED.** Switch closed Trinity's two gaps: (1) wired RefreshBackgroundCoreAsync callsite (line 1608), (2) added forward-slash trim `TrimEnd('\\', '/')` (line 222). Contract compliance exact. All 9 targeted tests green (4 Tank + 5 Dozer). Full suite 958/958 green. Both MainForm callsites verified with correct ordering. Test cleanup in CwdTests (reflection removal, direct API calls) accepted as quality improvement. Format clean. No scope creep. **Principle: When a gap closure requires a second implementer, the binding contract must not change; the new implementer fills in what the first missed, nothing more.**

- **2026-05-17 — TDD gate review (Cache-Free CWD RED suite): APPROVED.** Combined Tank + Dozer RED suite: 31 tests across 3 classes. 19 fail for correct reasons (missing cwd resolution, caches still present, nested quote bug). 12 pass for correct reasons (yaml-only behavior that must survive implementation). Source-contract guards collectively adequate: field-name + implementation-shape + API-name guards together resist renames. Performance budget (500KB/100ms for 8MB file) discriminates tail-read from full-read. Integration test stale references caught by `Tests_Source_DoesNotReferenceRemovedLiveCwdOverlayApis`. Implementer assigned: Switch. **Principle: A source-contract guard suite is collectively tight when no single rename can bypass ALL guards simultaneously; individual rename vulnerability is acceptable if the ensemble catches the structural fingerprint.**

